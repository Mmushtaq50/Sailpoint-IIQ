  

String displayName = identity.getDisplayName();
         
  
           if(displayName == null || displayName.isEmpty()){
            	displayName = identity.getFirstname()+"."+identity.getLastname();
            }
            return displayName;
