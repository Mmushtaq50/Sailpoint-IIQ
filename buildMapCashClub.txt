  import java.util.Map;
  import java.util.HashMap;
  import sailpoint.api.SailPointContext;
  import sailpoint.connector.DelimitedFileConnector;

  Map map = DelimitedFileConnector.defaultBuildMap(cols, record);

  Map mp = new HashMap();

  System.out.println("map"+map);

  String empid = (String) map.get("Employee ID");
  String onid = (String) map.get("Online ID");
  String usercde = (String) map.get("User Code");
  String fname = (String) map.get("First Name");
  String lname = (String) map.get("Last Name");
  String status = (String) map.get("Status");
  String accgrp = (String) map.get("Access Group");


  if(empid != null &amp;&amp; !empid.equalsIgnoreCase("ngross2")){
    mp.put("Employee ID", empid);
    mp.put("Online ID", onid);
    mp.put("User Code", usercde);
    mp.put("First Name",  fname);
    mp.put("Last Name", lname);
    mp.put("Status", status);
    mp.put("Access Group", accgrp);
  }
  return mp;